{
  "name": "steady-ahh",
  "version": "0.1.0",
  "private": true,
  "description": "Steady-Ahh — calm wellness web app (Foundation scaffold). Root is orchestration only; app code lives in client/ and server/.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "test": "npm run test --workspace=client && npm run test --workspace=server",
    "test:e2e": "playwright test --config=e2e/playwright.config.cjs",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "typecheck": "node -e \"console.log('JavaScript project: no TypeScript typecheck configured')\""
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "concurrently": "^9.0.1"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
