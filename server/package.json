{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Steady-Ahh Express API (Foundation scaffold).",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:migrate": "node src/db/migrate.js",
    "test": "vitest run",
    "lint": "oxlint",
    "build": "node -e \"console.log('Server has no build step — runs directly on Node.')\""
  },
  "dependencies": {
    "@clerk/express": "^2.1.69",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "pg": "^8.13.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "oxlint": "^1.81.0",
    "supertest": "^7.0.0",
    "vitest": "^5.0.1"
  }
}
